/* ============================================================
   PLACEHOLDER DATA — DAILY AFFIRMATION
   ------------------------------------------------------------
   In production this single record should be fetched from an
   admin-managed endpoint (e.g. GET /api/affirmations/today) so
   an administrator can update it without touching the frontend.
   The text below is SAMPLE content only.
   ============================================================ */
window.DC_DATA = window.DC_DATA || {};

window.DC_DATA.dailyAffirmation = {
  isPlaceholder: true,
  text: "United we stand: today I show up for my brothers with honesty, discipline and the quiet strength this Club was built on.",
  date: "Today"
};
